[build-system]
# Minimum requirements for the build system to execute.
requires = [
  "setuptools",
  "wheel",
  "pyarrow>=12.0.0",
  "pandas>=1.1",
  "numpy>=1.19.1",
]

[tool.black]
target-version = ['py37', 'py311']
exclude = '''
(
  | \.egg
  | \.git
  | build
  | setup.py
)
'''
